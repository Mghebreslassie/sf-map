import { MainContainer, PositionContainer } from "../styles/charcterboxstyle";

const CharacterBox: React.FC = () => {
  return (
    <PositionContainer>
      <MainContainer></MainContainer>
    </PositionContainer>
  );
};

export default CharacterBox;
